# QCloudSMS Server Doc

## Install
1. `npm install qcloudsmsserver.js --save`
2. clone this project 
## Usage
Load:  `var sms = require('qcloudsmsserver);`
Use: `let code = sms.SMSVerify(phoneNumber);`
